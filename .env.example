# Copy to .env and edit for your QNAP.

# Host that the browser uses to reach apps. Tile links point here.
PORTAL_EXTERNAL_HOST=http://qnap.local

# GID of the host's docker group. Find with:
#   stat -c '%g' /var/run/docker.sock
DOCKER_GID=999

# Docker network shared with the other homelab apps.
HOMELAB_DOCKER_NETWORK=homelab
